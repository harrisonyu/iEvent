
= form_for @sponsor, :url => { :action => "create"}, :html => {:class => "nifty_form"} do |f|
	= f.label "Sponsor Name"
	= f.text_field :sponsorName
	%br
	%br

	=f.submit "Add Sponsor", :class => "btn btn-success"

%br
<font = 5> Available Sponsors to Choose From: </font>
%br
%br
-@sponsors.each do |h|
	%tr
		%td
			= link_to h.name, show_person_path(h.id), class: "linkify-td"
%br
%br
%br
= link_to "Create New Person", new_person_path, class: "btn"
%br
%br
=link_to "Back", show_event_path, :class => "btn btn-primary"