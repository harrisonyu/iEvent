
= form_for @host, :url => { :action => "create"}, :html => {:class => "nifty_form"} do |f|
	= f.label "Host Name"
	= f.text_field :hostName
	%br
	%br

	=f.submit "Add Host", :class => "btn btn-success"

%br
<font = 5> Available Hosts to Choose From: </font>
%br
%br
-@hosts.each do |h|
	%tr
		%td
			= link_to h.name, show_person_path(h.id), class: "linkify-td"
%br
%br
%br
= link_to "Create New Person", new_person_path, class: "btn"
%br
%br
=link_to "Back", show_event_path, :class => "btn btn-primary"