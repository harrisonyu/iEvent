%br
= link_to "Create a new event", new_event_path, class: "btn btn-success"

%br
%br

/= link_to "Search", search_home_path, class: "btn btn-inverse"
%br
%br
%br

%table.table.table-bordered
  %thead
    %tr               
      %th Name:
      %th Add to MyEvents
  %tbody
    - @events.each do |e|
      %tr
        %td 
          = link_to e.name, show_event_path(e.id), class: "linkify-td"
        %td
          = link_to "Add to MyEvents", add_myevent_path(e.id), class: "btn"
%br


%br
<font = 5> People: </font>
%br
%br
-@persons.each do |h|
  %tr
    %td
      = link_to h.name, show_person_path(h.id), class: "linkify-td"
%br
%br
%br
= link_to "Create New Person", new_person_path, class: "btn"
%br
%br
%br
=link_to "Back", root_path, class: "btn btn-primary"
%br
%br
%br
%br
%br